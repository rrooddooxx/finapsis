{
  "name": "@finapsis/api",
  "version": "1.0.0",
  "description": "Finapsis Backend API",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "start": "bun run dist/index.js",
    "test": "bun test",
    "lint": "echo 'Linting API...' && exit 0"
  },
  "dependencies": {
    "@ai-sdk/openai": "^2.0.0",
    "ai": "^5.0.0",
    "bullmq": "^5.56.9",
    "hono": "^4.8.9",
    "zod": "^4.0.14"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.3"
  }
}