meta {
  name: send message
  type: http
  seq: 1
}

post {
  url: {{API_HOST}}/api/chat
  body: json
  auth: inherit
}

body:json {
  {
    "message": "como va la uf?"
  }
}

settings {
  encodeUrl: true
}
